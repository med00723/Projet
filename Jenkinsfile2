pipeline {

  agent any

  stages {

    stage('Checkout Source') {
      steps {
        git 'https://github.com/med00723/Projet.git'
      }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
   }
  }
}
